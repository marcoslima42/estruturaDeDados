#include <bits/stdc++.h>

using namespace std;
int main() {
    int n = 4;
    vector<int> v(n, 0);// criando com o tamanho de n e iniciando com 1
    //printf("Ola mundo\n\n");
    v.push_back(3);//adicionar no fim do vetor um novo elemento
    for (int i = 0; i < v.size(); i++) {
        printf(" %d ", v[i]);
    }

    //.size tamanho do vector
    //pair criar um conjunto pair<int,ll> p
    //max/min(1,2) max({1,2,3});
    //set
    
    return 0;
}