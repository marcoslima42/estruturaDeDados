#include <stdio.h>
#include <stdlib.h>

int main(){
    int tam=0;
    scanf("%d", &tam);
    char i[tam], p[tam];

    for(int i=0; i<tam; i++){
        scanf("%s %s", &i, &p);

    }

    
    return 0;
}